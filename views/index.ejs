<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <link rel="stylesheet" href="/style.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 2rem;
      background: #f8f9fa;
    }

    h1, h2 {
      color: #2c3e50;
    }

    .section {
      margin-bottom: 2.5rem;
      background: white;
      padding: 1.5rem;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    ul {
      padding-left: 1.2rem;
    }

    .timeline-item {
      margin-bottom: 0.75rem;
    }

    .tag {
      display: inline-block;
      font-size: 0.7rem;
      padding: 2px 6px;
      border-radius: 5px;
      color: white;
      margin-left: 5px;
    }

    .wow { background-color: #6a0dad; }
    .personal { background-color: #f39c12; }
    .education { background-color: #3498db; }
    .project { background-color: #2ecc71; }
    .career { background-color: #e67e22; }
  </style>
</head>
<body>
  <h1>👋 Hi, I'm <%= name %></h1>
  <p>I'm a project manager turned developer building full-stack apps and documenting my journey here.</p>

  <div class="section">
    <h2>🕒 Latest Milestones</h2>
    <ul>
      <% recentMilestones.forEach(m => { %>
        <li class="timeline-item">
          <strong><%= m.date %></strong> — <%= m.title %>
          <% if (m.type) { %>
            <span class="tag <%= m.type %>"><%= m.type %></span>
          <% } %>
        </li>
      <% }) %>
    </ul>
    <a href="/timeline">→ View Full Timeline</a>
  </div>

  <div class="section">
    <h2>🎯 Goals</h2>
    <ul>
      <% goals.forEach(goal => { %>
        <li><%= goal %></li>
      <% }) %>
    </ul>
  </div>

  <div class="section">
    <h2>🎮 Hobbies</h2>
    <ul>
      <% hobbies.forEach(hobby => { %>
        <li><%= hobby %></li>
      <% }) %>
    </ul>
    <a href="/hobbies">→ See More</a>
  </div>

  <div class="section">
    <h2>💻 Projects</h2>
    <p>I'm always building! Here’s what I’ve worked on recently:</p>
    <a href="/projects">→ View My Projects</a>
  </div>
</body>
</html>